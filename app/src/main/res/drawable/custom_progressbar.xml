<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/color_hint_interact"
                android:centerColor="@color/color_hint_interact"
                android:centerY="1.0"
                android:endColor="@color/color_hint_interact"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/color_text"
                    android:centerColor="@color/color_text"
                    android:centerY="1.0"
                    android:endColor="@color/color_text"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>